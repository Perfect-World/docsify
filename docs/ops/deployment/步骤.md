1.准备.md

2.文件目录.md

3.docker.md

4.mysql.md

5.导入数据

6.启动后端

7.启动前端



```bash
数据库同步，1，通过服务器上的navicat工具，数据库名scsmp_prod，先同步结构，再同步指定部门表的数据，可选择已经保存的配置文件，数据同步一定要注意，特别是删除一列，，，还要注意选择的表
2.源代码中写好sql文件，提交git后，有nginx下载sql文件路径，服务器上下载后，进入docker的mysql容器中，执行sql文件
源代码sql文件目录，document/sql/sync
nginx下载sql文件路径, http://121.237.178.252/scsmp_sql/prod2025.sql
sudo docker cp /tmp/scsmp_$(date +"%Y%m%d").sql mysql8.2:/tmp/scsmp_$(date +"%Y%m%d").sql
sudo docker exec -it mysql8.2 /bin/bash
mysql -uyykj -P3306 -pHh6@yykj scsmp_prod < /tmp/scsmp_$(date +'%Y%m%d').sql

后端部署，sh /yykj/project/scsmp-java/deploy_scsmp_java.sh
后端日志 tail -f -n 100 /yykj/project/scsmp-java/logs/server.log
前端部署，sh /yykj/project/scsmp-vue/deploy_scsmp_vue.sh

不能上网的服务器，数据库部署，1.导出测试服务器上scsmp_prod库结构和数据结构，2.上传到部署服务器，打开navicat后建数据库执行sql文件，3.进行同步即可
如药科192.168.200.245中yykj数据库

后端前端部署文件，需要运维机下载
http://121.237.178.252/scsmp_java_preprod/admin_java_preprod.jar
http://121.237.178.252/scsmp_vue_preprod/dist.zip
下载后，后面加上日期名admin_java_preprod20251230.jar   dist20251230.zip
上传到部署服务器，继续执行sh脚本


宁波大学 
sh /yykj/project/scsmp-java/deploy_scsmp_java.sh大
tail -f -n 100 /yykj/project/scsmp-java/logs/server.log大
sh /yykj/project/scsmp-vue/deploy_scsmp_vue.sh大

数据同步一定要注意，特别是删除一列，，，还要注意选择的表
所有命令先切换root：    sudo -i


    # 查看防火墙开放端口
firewall-cmd --zone=public --list-ports
     # 开放端口
firewall-cmd --zone=public --add-port=50001-50200/tcp --permanent
     # 重新加载
firewall-cmd --reload
```